machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb

dependencies:
  override:
    - ./gradlew resolveConfigurations --parallel

test:
  override:
    - ./gradlew test --info:
        parallel: true
        timeout: 1200

